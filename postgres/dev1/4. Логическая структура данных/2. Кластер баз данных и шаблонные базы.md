### 📌 **Подробный конспект – Часть 2: Кластер баз данных и шаблонные базы**

#### 🔹 **Что такое кластер баз данных?**

- **Кластер баз данных** – это **группа баз данных**, управляемых одним экземпляром сервера PostgreSQL.
- Перед началом работы **кластер необходимо инициализировать**.
- При инициализации **по умолчанию создаются три базы данных**:
    1. `postgres` – стандартная база данных для работы с сервером.
    2. `template0` – шаблонная база, всегда остаётся **чистой** (без изменений).
    3. `template1` – шаблонная база, **из которой по умолчанию создаются новые базы данных**.

#### 🔹 **Шаблонные базы данных (`template0` и `template1`)**

##### **1. `template1` (основной шаблон)**

- Все новые базы данных **создаются путём копирования** `template1`, если не указано иное.
- Можно **модифицировать** `template1`, добавляя в неё объекты (например, расширения, схемы, таблицы).
- Если добавить нужные настройки в `template1`, они **автоматически появятся во всех новых базах**.

##### **2. `template0` (чистый шаблон)**

- Это **чистая** база данных, **которую нельзя изменять**.
- Используется, когда:
    - Нужно создать **базу данных с другой кодировкой**.
    - Требуется создать **пустую** базу для восстановления из бэкапа (`pg_restore`).

##### 📌 **Создание базы данных (клонирование шаблона)**

```sql
CREATE DATABASE new_db;  -- Создаст копию `template1`
CREATE DATABASE new_db TEMPLATE template0;  -- Создаст копию `template0`
```

#### 🔹 **Обычная база данных (`postgres`)**

- Это стандартная база данных, созданная при инициализации.
- Можно подключаться и работать с ней, но её **можно удалить**, если созданы другие базы.

#### 🔹 **Создание и удаление баз данных**

📌 **Создать базу данных**:

```sql
CREATE DATABASE my_database;
```

📌 **Удалить базу данных (если нет активных подключений)**:

```sql
DROP DATABASE my_database;
```

Если база данных занята, перед удалением нужно **переключиться на другую**:

```sql
\c postgres  -- Переключиться в другую базу
DROP DATABASE my_database;
```

#### 🔹 **Сколько баз данных создавать?**

- PostgreSQL **не ограничивает** число баз данных.
- Решение зависит от архитектуры приложения:
    - Одна база на проект.
    - Разные базы для разных окружений (разработка, тест, прод).
    - Отдельные базы для разных клиентов (multi-tenant архитектура).

#### 🔹 **Пример использования нескольких баз данных**

- В обучающих курсах часто создаётся **новая база данных для каждой темы**, чтобы:
    - Изолировать настройки.
    - Избежать конфликтов между объектами.
    - Чётко разделять эксперименты.

📌 **Проверить список баз данных в `psql`**:

```sh
\l
```

📌 **Подключиться к базе данных**:

```sh
\c my_database
```

---

✅ **Далее: Часть 3 – Структура базы данных: схемы**. Готов продолжать? 🚀